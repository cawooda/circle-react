{
  "name": "circle-independent",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "node src/server/src/index.js",
    "develop": "concurrently \"cd src/server && npm run dev\" \"cd src/client && npm run dev\"",
    "install": "cd src/server && npm install && cd src/client && npm install",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
